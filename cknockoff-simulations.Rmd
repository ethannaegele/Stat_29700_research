---
title: "cknockoff-sims"
author: "Ethan Naegele"
date: "2024-08-02"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
run_CKnockoffs_simulation <- function(type = 'MCC', n, d, a, num_true_sigs, alpha = .05, num_iter = 500){
  # Sets up the regression problem with specified dimensions and number of true signals, all of which have
  # the same amplitude. 
  # Runs the simulation by generating the regression problems and then runs CONDITIONAL KNOCKOFFS 
  
  fdp_vec <- numeric(num_iter)
pwr_vec <- c(num_iter)
for (m in 1:num_iter){
  feature_indices <- 1:d
  regression <- get_regression_model(n = n, d = d, a = a, num_true_signals = num_true_sigs, random_true_signal_indices = TRUE)
  true_nonzero_indices <- regression$nonzero_indices
  rejected_indices <- cknockoff(regression$X, 
                                regression$Y,
                                intercept = FALSE,
                                alpha = alpha)$selected
  num_rejections <- length(rejected_indices)
  num_correct_rejections <- length(intersect(rejected_indices, true_nonzero_indices))
  num_false_rejections <- length(intersect(feature_indices[-true_nonzero_indices], rejected_indices))
  fdp_vec[m] <- num_false_rejections / max(num_rejections, 1)
  pwr_vec[m] <- num_correct_rejections / max(length(true_nonzero_indices), 1)
}
fdr <- mean(fdp_vec)
pwr <- mean(pwr_vec)
return(list(fdr = fdr, power = pwr))
}
```

```{r}
run_CKnockoffs_setting1_simulation <- function(a_vec = c(2, 4, 6, 8, 10), 
                                               num_iter = 500){
result_vec_power <- c()
result_vec_fdr <- c()
for (amp in a_vec){
  result <- run_CKnockoffs_simulation(n = 200,
                                          d = 40,
                                          a = amp, 
                                          num_true_sigs = 8,
                                          num_iter = num_iter)
  result_vec_power <- c(result_vec_power, result$power)
  result_vec_fdr <- c(result_vec_fdr, result$fdr)
}
return(data.frame(a = a_vec, power = result_vec_power, fdr = result_vec_fdr))
}
```

```{r}
run_CKnockoffs_setting2_simulation <- function(a_vec = c(2, 4, 6, 8, 10), 
                                               num_iter = 500){
result_vec_power <- c()
result_vec_fdr <- c()
for (amp in a_vec){
  result <- run_CKnockoffs_simulation(n = 500,
                                          d = 50,
                                          a = amp, 
                                          num_true_sigs = 10,
                                          num_iter = num_iter)
  result_vec_power <- c(result_vec_power, result$power)
  result_vec_fdr <- c(result_vec_fdr, result$fdr)
}
return(data.frame(a = a_vec, power = result_vec_power, fdr = result_vec_fdr))
}
```


```{r}
run_CKnockoffs_setting3_simulation <- function(a_vec = c(2, 4, 6, 8, 10), 
                                               num_iter = 500){
result_vec_power <- c()
result_vec_fdr <- c()
for (amp in a_vec){
  result <- run_CKnockoffs_simulation(n = 1000,
                                          d = 100,
                                          a = amp, 
                                          num_true_sigs = 20,
                                          num_iter = num_iter)
  result_vec_power <- c(result_vec_power, result$power)
  result_vec_fdr <- c(result_vec_fdr, result$fdr)
}
return(data.frame(a = a_vec, power = result_vec_power, fdr = result_vec_fdr))
}
```


```{r}
# kth moment of F(1, n) distribution limit as n to infinity
f_distribution_kth_moment_limit <- function(k){
  return(2^k * gamma(.5 + k) / sqrt(pi))
}
  
```

```{r}
f_distribution_kth_moment_limit(1)
f_distribution_kth_moment_limit(2)
f_distribution_kth_moment_limit(3)
f_distribution_kth_moment_limit(4)
f_distribution_kth_moment_limit(5)
f_distribution_kth_moment_limit(6)
```
```{r}
DoubleFactorial(1)
DoubleFactorial(3)
DoubleFactorial(5)
DoubleFactorial(7)
DoubleFactorial(9)
DoubleFactorial(11)
```


